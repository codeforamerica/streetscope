web:
  build: .
  command: honcho start -p 5000
  ports:
      - "5000:5000"
  volumes:
      - .:/code
  links:
    - elasticsearch
  environment:
    - ELASTICSEARCH_HOST='elasticsearch'
elasticsearch:
  image: elasticsearch
  ports:
      - "9200:9200"

